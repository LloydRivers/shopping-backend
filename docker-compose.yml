version: '3.9'

services:
  postgresql:
    image: postgres
    init: true
    container_name: postgresql
    restart: always
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: shopping-api-services
      PGUSER: postgres
    ports:
      - 5432:5432
    volumes:
      - ./postgres:/var/lib/postgresql/data
    networks:
      - shopping-api-service-network

  shopping-api-services:
    container_name: shopping-api-services
    ports:
      - '8080:8080'
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - postgresql
    volumes:
      - ./app:/app
    command: ['npm', 'run', 'dev']
    networks:
      - shopping-api-service-network

volumes:
  postgres:
    driver: local

networks:
  shopping-api-service-network:
    driver: bridge
